# Set the usb audio module index to -2 so that it will not be set as the primary card
# options snd_usb_audio index=-2

# Set the usb audio module to be the #2 card (cat /proc/asound/modules) 
options snd_usb_audio index=2,0
